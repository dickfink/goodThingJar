<% include partials/header %>
  <% include partials/navbar %>

    <div class="form-memory">
      <h1>
        edit a memory
      </h1>
      <form action="../<%= posts._id %>?_method=PUT" method="POST">
        <div class="form-group">
          <input class="form-control form-control-lg" type="text" name="memory" value="<%= posts.memory %>">
          <button class="btn btn-primary float-right" type="submit">re-Remember</button>
        </div>
      </form>
      <form action="/<%= posts._id %>?_method=DELETE" method="POST">
        <button class="btn btn-sm btn-danger" type="submit">Delete</button>
      </form>
    </div>

    <% include partials/footer %>